//Groove
pipeline {
    agent any

    stages {
        stage('Begin') {
            steps {
                script {
                    dockerapp = docker.build("eduardolss/jenkins-test", '-f ./src/Dockerfile ./src')
                }
            }
        }
    }
}